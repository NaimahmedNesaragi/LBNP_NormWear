# -----------------------------
# Core Python libraries
# -----------------------------
tqdm>=4.65.0
pandas>=2.1.0
tabulate>=0.9.0
scikit-learn>=1.3.0
scipy>=1.11.0
sympy>=1.12

# -----------------------------
# PyTorch (GPU ready)
# -----------------------------
# NOTE: Install PyTorch + torchvision + torchaudio using conda for CUDA
# Example:
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# -----------------------------
# Vision / Transformer / ML
# -----------------------------
timm>=0.9.0
transformers>=4.40.0

# -----------------------------
# Baselines / extra models
# -----------------------------
#git+https://github.com/amazon-science/chronos-forecasting.git
#git+https://github.com/lukemelas/PyTorch-Pretrained-ViT.git
